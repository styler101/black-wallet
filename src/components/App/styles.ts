import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",

    backgroundColor: "#111",
  },

  header: {
    alignItems: "center",
    justifyContent: "center",
  },

  image: {
    width: 180,
    height: 50,
    resizeMode: "contain",
    marginVertical: 120,
  },

  main: {},

  footer: {},
});
